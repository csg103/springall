<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csg.cn.db.dao.PartyMemberScoreMapper">
    <resultMap id="BaseResultMap" type="com.csg.cn.db.entity.PartyMemberScore">
        <!--
          WARNING - @mbg.generated
        -->
        <result column="partMemberscoreId" jdbcType="CHAR" property="partmemberscoreid"/>
        <result column="partymemberId" jdbcType="CHAR" property="partymemberid"/>
        <result column="score" jdbcType="CHAR" property="score"/>
        <result column="year" jdbcType="DATE" property="year"/>
    </resultMap>
    <sql id="Example_Where_Clause">
        <!--
          WARNING - @mbg.generated
        -->
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="("
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <select id="countByExample" parameterType="com.csg.cn.db.entity.PartyMemberScoreExample"
            resultType="java.lang.Long">
        <!--
          WARNING - @mbg.generated
        -->
        select count(*) from t_partymember_score
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
    </select>


    <insert id="insertList" parameterType="java.util.List" useGeneratedKeys="false">
        insert into t_partymember_score (
        partMemberscoreId, partymemberId ,score ,year
        ) values <foreach
                collection="list" item="item" index="index" separator=",">(
        #{item.partmemberscoreid}, #{item.partymemberid},
            #{item.score}, #{item.year} )
        </foreach>
    </insert>


</mapper>