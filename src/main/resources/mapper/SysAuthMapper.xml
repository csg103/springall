<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csg.cn.db.dao.SysAuthMapper">
  <resultMap id="BaseResultMap" type="com.csg.cn.db.entity.SysAuth">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="auth_name" jdbcType="VARCHAR" property="authName" />
    <result column="auth_url" jdbcType="VARCHAR" property="authUrl" />
    <result column="auth_permission" jdbcType="VARCHAR" property="authPermission" />
    <result column="auth_type" jdbcType="VARCHAR" property="authType" />
    <result column="auth_status" jdbcType="VARCHAR" property="authStatus" />
    <result column="auth_description" jdbcType="VARCHAR" property="authDescription" />
    <result column="auth_parent_id" jdbcType="VARCHAR" property="authParentId" />
    <result column="auth_order" jdbcType="INTEGER" property="authOrder" />
  </resultMap>

  <resultMap id="MenuTreeResultMap" type="com.csg.cn.sys.tree.MenuTree" extends="BaseResultMap">
    <!--
      WARNING - @mbg.generated
    -->
    <result property="userId" jdbcType="VARCHAR" column="user_id"/>
    <collection property="subMenu" select="selectMenu" column="{id=id,userId=user_id}"/>
  </resultMap>
  <resultMap id="SysAuthCount" type="com.csg.cn.db.vo.SysAuthCount" extends="BaseResultMap">
    <result column="count" jdbcType="INTEGER" property="count"/>
  </resultMap>
  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
    -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
    -->
    id, auth_name, auth_url, auth_permission, auth_type, auth_status, auth_description, 
    auth_parent_id, auth_order
  </sql>
  <select id="countByExample" parameterType="com.csg.cn.db.entity.SysAuthExample" resultType="java.lang.Long">
    <!--
      WARNING - @mbg.generated
    -->
    select count(*) from t_sys_auth
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>

  <select id="selectListByRoleId" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    a.id, a.auth_name, a.auth_url, a.auth_permission, a.auth_type, a.auth_status, a.auth_description,
    a.auth_parent_id
    FROM t_sys_auth a
    LEFT JOIN t_sys_role_auth b ON a.id = b.auth_id
    <where>
      b.role_id = #{_parameter}
    </where>
  </select>

  <select id="selectMenu" resultMap="MenuTreeResultMap">
    select distinct c.*, a.user_id from t_sys_role_user a
    left join t_sys_role_auth b on a.role_id = b.role_id
    left join t_sys_auth c on b.auth_id = c.id
    where c.auth_status = '1' and c.auth_type = '0'
    and a.user_id = #{userId} and c.auth_parent_id = #{id}
    order by c.auth_order desc
  </select>

  <select id="selectAuthWithSubCount" resultMap="SysAuthCount">
    select *, (select count(1) from t_sys_auth a where b.id = a.auth_parent_id) as count
    from t_sys_auth b where b.auth_parent_id = #{authParentId};
  </select>
</mapper>