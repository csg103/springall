<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csg.cn.db.dao.SysUserMapper">
  <resultMap id="BaseResultMap" type="com.csg.cn.db.entity.SysUser">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="salt" jdbcType="VARCHAR" property="salt" />
    <result column="user_status" jdbcType="VARCHAR" property="userStatus" />
    <result column="dept_id" jdbcType="VARCHAR" property="deptId" />
  </resultMap>

  <resultMap id="SysUserSimple" type="com.csg.cn.db.vo.SysUserSimple">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="role" jdbcType="VARCHAR" property="role" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
    -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
    -->
    id, username, name, password, phone, salt, user_status, dept_id
  </sql>
  <select id="countByExample" parameterType="com.csg.cn.db.entity.SysUserExample" resultType="java.lang.Long">
    <!--
      WARNING - @mbg.generated
    -->
    select count(*) from t_sys_user
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>

  <select id="selectList" resultMap="SysUserSimple">
    select a.id, a.name, a.username, d.role_description as role ,a.phone from t_sys_user a
    left join t_sys_role_user c on a.id = c.user_id
    left join t_sys_role d on c.role_id = d.id
    <where>
      <if test="searchName!=null and searchName!=''">
        a.name like concat('%', #{searchName}, '%')
      </if>
    </where>
  </select>

  <select id="selectUserInfo" resultMap="SysUserSimple">
    select a.id, a.name, a.username, d.id as role ,a.phone from t_sys_user a
    left join t_sys_role_user c on a.id = c.user_id
    left join t_sys_role d on c.role_id = d.id
    <where>
      a.id = #{id}
    </where>
  </select>
</mapper>